<%= form_for(@id) do |f| %>
  <% if @id.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@id.errors.count, "error") %> prohibited this id from being saved:</h2>

      <ul>
      <% @id.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
